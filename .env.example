# Seyaj Security Guard Management System
VITE_PORT=3000
VITE_API_BASE_URL=
BACKEND_PORT=8000

# Supabase production project
VITE_SUPABASE_URL=https://umvzzzivlpucsdiwubaw.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_OR_PUBLISHABLE_KEY

# Authentication
# Set to supabase when using Supabase Auth; legacy keeps the existing backend/OIDC flow.
VITE_AUTH_MODE=supabase
# Keep false during migration. Set true only after creating the admin profile and confirming the login flow.
VITE_REQUIRE_AUTH=false
